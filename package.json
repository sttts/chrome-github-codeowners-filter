{
  "name": "chrome-github-codeowners-filter",
  "version": "0.1.2",
  "private": true,
  "description": "Chrome extension for filtering GitHub pull request files by CODEOWNER groups.",
  "scripts": {
    "package": "bash scripts/package.sh",
    "test": "node --test tests/*.test.cjs"
  }
}
